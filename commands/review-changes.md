Invoke the `implementation-review` skill.

**Scope:** Uncommitted changes (staged + unstaged).

Run `git diff HEAD` to enumerate what changed. If an intent is provided below, use it; otherwise infer from diff.

$ARGUMENTS
