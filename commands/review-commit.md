Invoke the `implementation-review` skill.

**Scope:** Committed changes. Use provided range, or default to `HEAD~1..HEAD`.

Run `git diff <range>` and `git log --oneline <range>` to enumerate what changed and why.

$ARGUMENTS
